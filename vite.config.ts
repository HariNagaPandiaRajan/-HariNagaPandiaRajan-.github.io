import { defineConfig } from 'vite';

export default defineConfig({
  base: '/-HariNagaPandiaRajan-.github.io/',
  build: {
    outDir: 'dist',
    assetsInlineLimit: 0,
  },
  publicDir: 'public',
});
